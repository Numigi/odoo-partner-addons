<?xml version="1.0"?>
<odoo>
    <data noupdate="1">

        <!-- avoid error as the parameter is already introduced by partner_firstname -->
        <function
                model="ir.config_parameter"
                name="set_param"
                eval="('partner_names_order.invitation_scope', 'first_last')"
        />

    </data>
</odoo>
