<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <record id="tree_res_partner_relation_all_extended" model="ir.ui.view">
    <field name="name">res.partner.relation.all.extended.tree</field>
    <field name="model">res.partner.relation.all</field>
    <field name="inherit_id" ref="partner_multi_relation.tree_res_partner_relation_all"/>
    <field name="arch" type="xml">

      <field name="other_partner_id" position="after">
        <field name="strength"/>
      </field>

      <field name="date_end" position="after">
        <field name="note"/>
      </field>

    </field>
  </record>

</odoo>
